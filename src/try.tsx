export {};

{
  /* <div>
        <input
          type="text"
          placeholder="Znajdz Produkt"
          onChange={handleChange}
          value={input}
        />
        {vegetables.map((veg) => {
          return <Product productName={veg} />;
        })}
      </div> */
}
